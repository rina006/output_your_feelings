<div class="container contact_new">
  <div class="row">
    <div class="col-md-10 offset-md-1 col-lg-8 offset-lg-2">
      <h2 class="text-center">お問い合わせフォーム</h2>
      <p class="text-center">MyEmotionへのご意見・ご感想・ご要望などのお問い合わせは<br>下記フォームよりお願いいたします</p>
      <div class="form-area">
        <div class="frame">
          <%= form_with model: @contact, local: true do |f| %>
            <%= render 'shared/error_messages', object: f.object %>
            <div class="mb-3">
              <%= f.label (t 'contacts.new.name'), class: 'form-label' %> <span class="badge bg-danger">必須</span>
              <%= f.text_field :name, class: 'form-control mb-3' %> 
            </div>
            <div class="mb-3">
              <%= f.label (t 'contacts.new.email'), class: 'form-label' %> <span class="badge bg-danger">必須</span>
              <%= f.email_field :email, class: 'form-control mb-3' %>
            </div>
            <div class="mb-3">
              <%= f.label (t 'contacts.new.content'), class: 'form-label' %> <span class="badge bg-danger">必須</span>
              <%= f.text_area :content, class: 'form-control mb-3' %>
            </div>
            <div class="text-center">
              <%= f.submit '送信', class: 'save-btn' %>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div> 
